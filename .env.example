# Строка подключения из Neon (Dashboard → Connection string)
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
# Длинный случайный секрет — станет частью URL коннектора: https://<app>/mcp/<MCP_SECRET>
MCP_SECRET=change-me-to-a-long-random-string
PORT=3000
